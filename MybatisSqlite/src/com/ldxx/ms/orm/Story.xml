<?xml version="1.0" encoding="gbk" ?>     
	<!DOCTYPE mapper     
	PUBLIC "-//ibatis.apache.org//DTD Mapper 3.0//EN"     
	"http://ibatis.apache.org/dtd/ibatis-3-mapper.dtd">
<mapper namespace="com.ldxx.ms.orm.Story">

	
	<update id="update" parameterType="com.ldxx.ms.orm.Story">
		update story
		<set>
		 	date_time = #{date_time,jdbcType=VARCHAR}
			
		</set>
		<where>
			pid = #{pid,jdbcType=VARCHAR}
		</where>
	</update>
	
	<select id="getStory" resultType="com.ldxx.ms.orm.Story">
		select pid,date_time,title,content from story order by date_time desc
	</select>

</mapper>